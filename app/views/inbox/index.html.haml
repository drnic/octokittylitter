.list
  %div{ :style => "float: right;" }
    = will_paginate @messages

  %h1 Inbox

  - if flash[:notice]
    .notification
      = flash[:notice]

  != render :partial => 'messages'

  %div{ :style => "float: right;" }
    = will_paginate @messages
